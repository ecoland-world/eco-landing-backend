{
	"name": "ecoland-backend",
	"version": "1.0.0",
	"description": "",
	"main": "dist/app.js",
	"scripts": {
		"prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
		"build": "tsc",
		"prestart": "npm run build",
		"start": "nodemon .",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "popcorn",
	"license": "MIT",
	"dependencies": {
		"@types/cors": "^2.8.12",
		"cors": "^2.8.5",
		"dotenv": "^16.0.2",
		"express": "^4.18.1",
		"nodemailer": "^6.7.8",
		"nodemon": "^2.0.19"
	},
	"devDependencies": {
		"@types/express": "^4.17.13",
		"@types/node": "16",
		"tslint": "^6.1.3",
		"typescript": "^4.8.2"
	}
}
